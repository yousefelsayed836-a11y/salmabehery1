name: Auto merge to main

on:
  push:
    branches:
      - 'claude/**'

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Merge to main
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout main
          git merge --strategy-option=theirs origin/${{ github.ref_name }} --allow-unrelated-histories -m "Auto-merge ${{ github.ref_name }} → main"
          git push origin main

      - name: Trigger Vercel deploy
        run: curl -X POST https://api.vercel.com/v1/integrations/deploy/prj_8BgJwNw3C3rG3DCwONxYxu18i0Le/bO5huAjJOV
